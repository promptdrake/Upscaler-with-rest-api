<!DOCTYPE html>
<html data-bs-theme="dark">
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    .custom-file-input {
      cursor: pointer;
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      var form = document.getElementById('formupload');
      var uploadButton = document.getElementById('uploadBtn');
      var convertingButton = document.querySelector('button[disabled]');

      uploadButton.addEventListener('click', function() {
        
        if (form.checkValidity()) {
          uploadButton.style.display = 'none';
          convertingButton.style.display = 'block';
          form.submit();
        } else {
      
          form.reportValidity();
        }
      });
    });
  </script>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-6">
        <div class="card mt-5">
          <div class="card-body">
            <h5 class="card-title text-center">Remini Freemium</h5>
            <p class="card-description text-center">Please use for education purpose<br>We are not responsible for anything you upload on our server</p>
            <form action="/upload" id="formupload" method="POST" enctype="multipart/form-data">
              <div class="mb-3">
                <input type="file" class="form-control custom-file-input" name="file" id="file" required>
                <br>
                <div class="form-group">
                  <select class="form-control" id="exampleFormControlSelect1" name="upscale" required>
                    <option selected disabled>📦 Resolution (Click here)</option>
                    <option value="2">2x</option>
                    <option value="4">4x</option>
                  </select>
                </div>
              </div>
              <div class="d-grid">
                <button class="btn btn-primary" id="uploadBtn">
                  <span id="picius" style="visibility: hidden;" class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                  <span id="status">Upload</span>
                </button>
                <button class="btn btn-primary" type="button" disabled style="display: none;">
                  <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                  Converting...
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="text-center justify-content-center" style="padding-top: 10px;">
    <code>Server Hosted With 💖 By aisbircube</code>
  </div>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</body>
</html>
